<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.talaeeandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AlertDialogAndPermissionActivity"></activity>
        <activity android:name=".ConnectingToTheInternetAcrivity"></activity>
        <activity android:name=".NavigationDrawerActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".WebViewActivity" />
        <activity android:name=".RecyclerViewActivity" />
        <activity android:name=".SharedPreferencesTaskFirstActivity" />
        <activity android:name=".LinearLayoutActivity" />
        <activity android:name=".Relative" />
        <activity android:name=".MainActivity" />
        <activity android:name=".IntentTaskFirstActivity" />
        <activity android:name=".IntentTaskSecondActivity" />

        <receiver android:name=".MyBroadcastReceiver">
            <intent-filter>
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>